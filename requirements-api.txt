# ── Production serving layer (FastAPI) ──
# Deliberately light: serves the precomputed serving/ bundle. NO geopandas,
# folium, streamlit, or the raw data — only what's needed to read the small
# per-tract store, run the RF predictor, and answer HTTP.
#
# Exact pins below MUST match requirements-pipeline.txt so the model.joblib
# trained in the build layer loads byte-compatibly here.
pandas==3.0.2
numpy==2.4.4
scikit-learn==1.8.0
joblib==1.5.3

# Web stack. Pinned to the versions installed and tested in Phase 1.
fastapi==0.136.1
uvicorn[standard]==0.47.0
pydantic==2.13.4
pydantic-settings==2.12.0
