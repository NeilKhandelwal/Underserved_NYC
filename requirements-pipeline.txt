# ── Batch pipeline + validation suite (the "build layer") ──
# Heavy geo / ML / stats stack. Runs offline against the ~16 GB raw data to
# produce output/master.geojson + the trained model. NOT shipped to prod.
#
# Pinned to the versions that currently produce the artifacts. The model
# (.joblib) is sensitive to the scikit-learn version, so keep that exact and
# in sync with requirements-api.txt.
#
# Follow-up (Phase 1): hash-lock with `uv pip compile` for fully reproducible builds.

geopandas==1.1.3
pyogrio==0.12.1          # geopandas vector IO backend
pyproj==3.7.2            # CRS reprojection (EPSG:2263 -> 4326)
shapely==2.1.2
pandas==3.0.2
pyarrow==23.0.1          # parquet engine for the Socrata/Census on-disk cache
numpy==2.4.4
scikit-learn==1.8.0      # KEEP IN SYNC with requirements-api.txt (model.joblib)
scipy==1.17.1
statsmodels==0.14.6
requests==2.33.1         # Census ACS API
joblib==1.5.3            # model persistence
libpysal==4.14.1         # spatial weights (validation: Moran's I)
esda==2.9.0              # spatial autocorrelation (validation: Moran's I)
